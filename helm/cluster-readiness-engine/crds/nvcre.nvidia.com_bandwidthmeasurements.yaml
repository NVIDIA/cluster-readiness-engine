---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: bandwidthmeasurements.nvcre.nvidia.com
spec:
  group: nvcre.nvidia.com
  names:
    kind: BandwidthMeasurement
    listKind: BandwidthMeasurementList
    plural: bandwidthmeasurements
    singular: bandwidthmeasurement
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BandwidthMeasurement is the Schema for the bandwidthmeasurements
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BandwidthMeasurement
            properties:
              jobRef:
                description: jobRef is the reference to the CRE Job for which bandwidth
                  should be measured.
                properties:
                  apiGroup:
                    description: |-
                      APIGroup is the group for the resource being referenced.
                      If APIGroup is not specified, the specified Kind must be in the core API group.
                      For any other third-party types, APIGroup is required.
                    type: string
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name is the name of resource being referenced
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
              logProfileRef:
                description: |-
                  logProfileRef is the name of the cluster-scoped LogProfile to use for log parsing.
                  The LogProfile defines a bandwidthResult regex pattern for extracting NCCL bandwidth data.
                minLength: 1
                type: string
              sampleInterval:
                description: |-
                  sampleInterval is how often to sample pod logs while the Job is running.
                  Default: 60s.
                type: string
              testType:
                description: |-
                  testType identifies the NCCL collective operation being measured (e.g., "all_reduce", "alltoall").
                  Used as the "nccl_test" Prometheus label to distinguish results on dashboards.
                type: string
            required:
            - logProfileRef
            type: object
          status:
            description: status defines the observed state of BandwidthMeasurement
            properties:
              completionTime:
                description: completionTime is the time when the measurement completed
                  (when the referenced Job reached a terminal state).
                format: date-time
                type: string
              conditions:
                description: |-
                  conditions represent the current state of the BandwidthMeasurement resource.

                  Condition types:
                  - "Measuring": the measurement is in progress (referenced Job is running)
                  - "Complete": the measurement has finished (referenced Job succeeded or failed)

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              results:
                description: |-
                  results contains per-message-size average bandwidth measurements.
                  Each entry represents the running average of all observed measurements for that size.
                items:
                  description: BandwidthResult stores the average bandwidth for a
                    single message size.
                  properties:
                    algBW:
                      description: algBW is the average algorithmic bandwidth in GB/s.
                      type: string
                    busBW:
                      description: busBW is the average bus bandwidth in GB/s.
                      type: string
                    samples:
                      description: samples is the number of measurements averaged
                        for this size.
                      type: integer
                    sizeBytes:
                      description: sizeBytes is the message size in bytes.
                      format: int64
                      type: integer
                  required:
                  - algBW
                  - busBW
                  - samples
                  - sizeBytes
                  type: object
                type: array
              startTime:
                description: startTime is the time when the measurement started (when
                  the referenced Job began running).
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
