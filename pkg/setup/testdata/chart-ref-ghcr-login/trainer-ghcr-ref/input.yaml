# The published Kubeflow Trainer chart ref is also GHCR-hosted. The trainer
# install path performs no registry login at all, but the helper must still
# classify its ref correctly.
chartRef: "oci://ghcr.io/kubeflow/charts/kubeflow-trainer"
