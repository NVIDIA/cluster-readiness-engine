# The published Kubeflow Trainer chart ref is also GHCR-hosted, so with an
# --image-pull-secret token the [deps] phase logs in to GHCR before pulling
# it (private trainer forks authenticate the same way as the NVCRE chart).
chartRef: "oci://ghcr.io/kubeflow/charts/kubeflow-trainer"
