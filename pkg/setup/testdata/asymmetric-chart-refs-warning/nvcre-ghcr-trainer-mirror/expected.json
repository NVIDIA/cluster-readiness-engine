{
  "warning": "[preflight] Warning: --trainer-chart-ref points at registry.example.com but --chart-ref still points at ghcr.io, so the [helm] phase pulls the NVCRE chart from ghcr.io. Mirror both charts."
}
