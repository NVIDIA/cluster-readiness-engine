# No Helm record (out-of-band install): the version is read from the Trainer
# controller Deployment image tag, which matches after trimming the leading v.
deploymentImage: ghcr.io/kubeflow/trainer/trainer-controller-manager:v2.2.1
